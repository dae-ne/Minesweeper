<Window x:Class="Minesweeper.Presentation.Views.GameView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper.Presentation.Views"
        mc:Ignorable="d"
        Title="GameView"
        Height="{Binding WindowHeight, Mode=TwoWay, FallbackValue=450, UpdateSourceTrigger=PropertyChanged}"
        Width="{Binding WindowWidth, Mode=TwoWay, FallbackValue=800, UpdateSourceTrigger=PropertyChanged}">
    <Grid>
        <Button x:Name="DoSomething"></Button>
    </Grid>
</Window>
