<UserControl x:Class="Minesweeper.Test.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:Minesweeper.Test.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:MenuViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Minesweeper" />
            <Button x:Name="EasyModeBtClick" Content="Easy" />
            <Button x:Name="MediumModeBtClick" Content="Medium" />
            <Button x:Name="HardModeBtClick" Content="Hard" />
            <Slider Value="{Binding BoardWidth, Mode=TwoWay}" />
            <Slider Value="{Binding BoardHeight, Mode=TwoWay}" />
            <Slider Value="{Binding NumberOfMines, Mode=TwoWay}" />
            <Button x:Name="StartGameBtClick" Content="Start game" />
        </StackPanel>
    </Grid>
</UserControl>
